% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/threadle_client.R
\name{th_load_examples}
\alias{th_load_examples}
\title{Load bundled example data}
\usage{
th_load_examples(
  examples = c("mynet", "lazega"),
  assign = TRUE,
  set_workdir = TRUE,
  envir = parent.frame()
)
}
\arguments{
\item{examples}{Character vector of example dataset names. Supported values
are \code{"mynet"}, \code{"lazega"}, \code{"lazega_female"}, \code{"dscw"}, and \code{"all"}.
Defaults to \code{c("mynet", "lazega")}.}

\item{assign}{Logical; if \code{TRUE}, assign handles such as \code{mynet_nodeset} and
\code{mynet} into the caller's environment.}

\item{set_workdir}{Logical; if \code{TRUE}, set Threadle's working directory to the
bundled \code{extdata} folder before loading.}

\item{envir}{Environment where handles are assigned when \code{assign = TRUE}.}
}
\value{
A named list of \code{threadle_nodeset} and \code{threadle_network} handles.
}
\description{
\code{th_load_examples()} loads one or more example datasets shipped with
\pkg{threadleR}. It sets Threadle's working directory to the package
\code{extdata} folder, loads each dataset's nodeset and network, assigns the
resulting handles into the caller's environment by default, and returns them
as a named list.
}
\examples{
\dontshow{if (th_is_available()) withAutoprint(\{ # examplesIf}
th_start_threadle()

th_load_examples("mynet")
th_info(mynet)

th_stop_threadle()
\dontshow{\}) # examplesIf}
}
